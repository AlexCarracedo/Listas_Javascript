function comprobar(lista, elemento) {
    for (var i=0; i<lista.length; i++) {
        if (elemento == lista[i]) {
            return true;
            a=1;
        }
    }
    if (a==0) {
        return false;
    }
}
